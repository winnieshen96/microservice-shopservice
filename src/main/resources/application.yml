server:
  port: 7500 # will start this service in this port

spring:
  application:
    # will register to consul using the following name
    # other services will lookup this service with this name
    name: shop-service
  cloud:
    consul:
      # this consul client is located in 7500
      port: 7500